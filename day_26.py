#import os, time

#for i in range(100):
#  print(i)
#  os.system("clear")

#print("Welcome")
#print("To")
#print("Replit")

#time.sleep(1)
#os.system("clear")

#username = input("Username: ")

""" from replit import audio
import os, time

def play():
  source = audio.play_file('audio.wav')
  source.paused = False # unpause the playback
  while True:
    # Start taking user input and doing something with it
    stop_playing = int(input("Press 2 anytime to stop playback and go back to the menu : "))
    if stop_playing == 2:
      source.paused = True
      return
    else:
      continue

while True:
  # clear the screen 
  os.system("clear")
  # Show the menu
  print("🎵 MyPOD Music Player")
  time.sleep(1)
  print("Press 1 to Play")
  time.sleep(1)
  print("Press 2 to Exit")
  time.sleep(1)
  print("Press anything else to see the menu again")
  # take user's input
  userInput = int(input())
  if userInput == 1:
    print("Playing some proper tunes!")
    play()
  elif userInput == 2:
    exit()
  else:
    continue
  # check whether you should call the play() subroutine depending on user's input """

